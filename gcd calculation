#include<stdio.h>
int main(){
    int n,m;
    printf("enter two no.:");
    scanf("%d",&n);
    scanf("%d",&m);
    while(n!=m){
        if(n>m){
            n=n-m;
        }
        else{
            m=m-n;
        }
    }
    printf("gcd of two number:%d",n);
    return 0;
}
